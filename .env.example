# Porta em que a API Fastify escuta dentro do container
PORT=3000

# Token do bot, gerado pelo @BotFather no Telegram
BOT_TOKEN=

# Chave da API do TMDB (https://www.themoviedb.org/settings/api), usada para
# buscar o pôster do filme. Opcional: sem ela o bot só não envia o pôster.
TMDB_API_KEY=
